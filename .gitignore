# ── Node ──────────────────────────────────────────────────────────────────────
node_modules
dist
dist-ssr
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs
*.log

# ── Environment files — never commit secrets ──────────────────────────────────
.env
.env.*
!.env.example

# ── Editor ────────────────────────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
venv/
venv_new/
.venv/
tf_env/

# ── ML — large files not suitable for git ─────────────────────────────────────
# Dataset images (hundreds of MB)
dogs_cats_split/
cats_set/
dogs_set/
train/
validation/
test/

# Trained model weights (large binary files)
saved_model/*.keras
saved_model/*.h5
cats_dogs_model.keras
*.keras

# Keep label JSON files (small, needed to run the app)
!saved_model/class_names.json
!saved_model/pets_class_names.json

# Temporary upload directory
uploads/

# ── Misc ──────────────────────────────────────────────────────────────────────
# Selfie photos and processed output — personal data, not for repo
selfies/
selfies_processed/
